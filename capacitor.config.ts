import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Capstone-Project-v2-Ionic',
  webDir: 'www'
};

export default config;
